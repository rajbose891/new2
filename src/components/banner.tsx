import { SButton, SFlex, SPageSection } from 'react-simple-ui-lib';

export function Banner() {
    return <SPageSection backgroundUrl={'102.png'} height={800} backgroundSize={'cover'} paddingTop={40} align={'center'} justify={'start'}></SPageSection>;
}
